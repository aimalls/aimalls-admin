import { IonContent } from "@ionic/react";
import { FC } from "react";

export interface iProps {}
export const AddActionForm: FC<iProps> = (props): JSX.Element => {
    return (
        <IonContent>
            
            {"AddActionForm"}
        </IonContent>
    )
};
export default AddActionForm;